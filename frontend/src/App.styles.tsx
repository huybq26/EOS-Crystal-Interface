import {makeStyles} from "@material-ui/core/styles";

export const AppStyles = makeStyles({
    main: {
        paddingLeft: "10px",
        paddingRight: "10px"
    },
    title: {
        paddingBottom: "15px"
    }
});
